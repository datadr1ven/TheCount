/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration options for Next.js can be added here
  // For example: experimental features, rewrites, etc.
};

export default nextConfig;
